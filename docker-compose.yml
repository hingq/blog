services:
  nextjs:
    image: blog:latest
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: blog
    restart: unless-stopped
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      NODE_ENV: production
